#ifndef __JITTER_WORKER_H__
#define __JITTER_WORKER_H__

// needs to include methods: start process for thread, task queue for polling, tree search run and calls to the main thread

class JitterWorker {

};

#endif